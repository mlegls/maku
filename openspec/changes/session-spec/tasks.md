# Tasks: session spec extraction

## 1. Extract

- [x] 1.1 Author the `session` delta spec; validate; commit.
- [x] 1.2 Rewrite docs/player.md: keep user-visible behavior, cite the spec, drop internals and the dangling design.md reference.
- [ ] 1.3 Archive; fill the installed spec's Purpose + `## Design` (snapshot thinning, shared event log with cursors) from the removed player.md prose; validate.
